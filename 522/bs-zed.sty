% My packages for writing Zed in LaTeX. Note that order is important.
\usepackage[pdfborder=false]{hyperref} % Remove borders from PDF links
\usepackage{amsmath}                   % for equation* environment
\usepackage{zed-csp}                   % for zed macros
\usepackage{times}                     % for nice-looking text
%\usepackage{fullpage}                  % for smaller margins
\usepackage[]{todonotes}                 % for todo notes: ( \usepackage[disable]{todonotes}
\setlength{\parindent}{0pt}            % for less indentation
\setlength{\parskip}{1em}

\newcommand{\tostart}[1]{\todo{#1}}
\newcommand{\tofinish}[1]{\todo[color=blue!40]{#1}}

%%% Use this to make the typechecker run, but not print junk.
%% \iftypechecker
%% ... (this stuff won't show up in the document)
%% \fi
\newif\iftypechecker
\typecheckerfalse
